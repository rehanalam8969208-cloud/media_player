name: my_music
description: "My Music - Premium Glass Theme Offline Music Player"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^6.0.0
  file_picker: ^8.0.0
  permission_handler: ^11.3.0
  cupertino_icons: ^1.0.2

flutter:
  uses-material-design: true
